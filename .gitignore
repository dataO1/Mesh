.direnv
core
pd-externals

rave-models
target
result
dist
patches/
ground-truth.json
bpm-comparison.json
analysis-export.json
vampnet/
reports
crates/mesh-core/src/bin/history_stats.rs

# Spotify Web API credentials (per-developer secret; load from
# ~/.config/mesh-research/spotify.env or this local fallback)
.spotify.env

# Spotipy stashes a fetched Bearer token here — never commit
.cache

# Claude Code session metadata (locks, scheduled tasks, etc.)
.claude/

# Python bytecode anywhere in the tree
__pycache__/
*.pyc

# One-off local notes / scratch
process

# V18 smoke artifacts — get regenerated by every smoke run
models/aggression-axes/*_SMOKE_TEST*.json
